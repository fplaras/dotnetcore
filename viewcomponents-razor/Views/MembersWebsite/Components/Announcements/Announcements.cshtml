@model IEnumerable<viewcomponents_razor.Models.AnnouncementModel>
@{
    Layout = null;
}


<div>
    <h4>Announcement List</h4>
    <hr />
    @{
        foreach (var announcement in Model)
        {
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => announcement.ID)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => announcement.ID)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => announcement.Author)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => announcement.Author)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => announcement.Text)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => announcement.Text)
                </dd>
            </dl>
        }
    }

</div>
<div>
    @*@Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
        <a asp-action="Index">Back to List</a>*@
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">Open modal for </button>

   <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <partial name="_AnnouncementModal" />
    </div>
    
</div>
